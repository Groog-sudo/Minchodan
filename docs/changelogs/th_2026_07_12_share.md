# th 브랜치 작업 공유 (2026-07-12)

## 작업자
태현 (th)

## 이번에 th에 push한 커밋

- `5df4d9c` feat(client,android): VisionCamera Frame Processor 등록 및 TFLite NMS 출력 정합, LAN 모드 전환
- `576e349` merge: origin/kb를 th에 통합 - 파이프라인 레이턴시 계측, 오탐 판정, LiDAR 프로브, SSE 계약 고정, 콘솔 지도 연동 복구

## 핵심 변경 요약

### 1. Android 네이티브 모듈 정비
- `ReflexFrameProcessorPlugin.kt` 신규 — VisionCamera Frame Processor `reflexFrameCapture` 플러그인
- `MinchodanCustomPackage.kt` 신규 — 커스텀 네이티브 모듈 패키지
- `AudioSessionBridgeModule.kt` 신규 — STT 녹음 AEC 브릿지(Android 대응)
- `MainApplication.kt` — 커스텀 패키지 등록 + FrameProcessorPluginRegistry 등록

### 2. TFLite 탐지 출력 포맷 정합
- `tfliteDetector.ts` — YOLO 26N 출력 채널 33 → 6 (NMS-enabled export 형태에 맞춤)
- 빈 배열/0점 탐지 결과 문제 해결

### 3. 네트워크 모드 ngrok → LAN 전환
- `config/index.ts` — NETWORK_MODE="lan", LAN_IP="192.168.1.103"
- kb 병합 후 EXPO_PUBLIC_* 환경변수 패턴 도입, 기본값은 lan/192.168.1.103 유지

### 4. 카메라 뷰 지도 패널 가드
- `CameraView.tsx` — navRoute 미수신 시 지도 패널/토글 숨김, 경로 해제 시 자동 닫힘

### 5. kb 브랜치 통합 (9커밋)
- 파이프라인 레이턴시 계측, 실시간 브로드캐스트 확장
- 오탐 판정(false_positive) 컬럼 추가
- 관리자 회원 등록 페이지
- iOS 카메라 180도 방향 반전 수정
- 이벤트 프레임 보존(frame_path)
- LiDAR 실거리 프로브 프로토타입 (iOS Pro 전용)
- SSE 이벤트 계약 고정, 인증 기본값 환경 분리(fail-closed)
- 콘솔 지도 연동 복구

## Android 빌드 환경 (이전 커밋 85e9503에서 반영됨)

- Android SDK/Studio/JDK 17 신규 설치
- `gradle.properties`에 `org.gradle.java.home` 고정
- `build.gradle`에 CMake `buildStagingDirectory` 우회 (Windows 긴 경로 250자 한도 대응)

## 실기기 테스트 상태

- 폰: 카메라 ACTIVE, TFLite 듀얼 모델 로드 성공
- FastAPI: 0.0.0.0:8000 Listen 정상
- Metro: 8081 Listen (LAN 접근은 방화벽 이슈, `adb reverse tcp:8081 tcp:8081`로 우회 중)
- WebSocket 재접속 확인 + TMAP 네비게이션 경로 안내 검증이 다음 단계

## 후속 작업 필요

- Redis 기동 (localhost:6379 연결 거부 중)
- faster-whisper small 모델 프리로드 실패 원인 점검
- YOLO26N_SEG 가중치 파일 존재 여부 재확인
- Metro LAN 포트 방화벽 개방 또는 adb reverse 영구 적용

## 검증

- `npx tsc --noEmit` 통과
- Android Gradle 설정 단계 통과
- FastAPI/Metro Listen 확인

## 브랜치 규칙

- 모든 작업은 th 브랜치에서만
- dev/main은 팀장 영역 (건드리지 않음)
- kb는 동료 브랜치, th에서 병합만 수행
