# Minchodan 서버 파이썬 의존성
# Python 3.13, CUDA 12.8 + cu128 PyTorch 휠 (Blackwell sm_120)

# --- Web 서버 ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-dotenv>=1.0.0

# --- 이미지 처리 ---
opencv-python>=4.10.0
numpy>=2.0.0
Pillow>=10.0.0

# --- 탐지·분할·추적 ---
ultralytics>=8.3.0
# PyTorch는 cu128 휠을 별도 설치 권장:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
torch>=2.5.0
torchvision>=0.20.0
# transformers (SegFormer) - 모델 학습 시 필요
transformers>=4.45.0

# --- Redis ---
redis>=5.0.0
aioredis>=2.0.0

# --- Vector DB ---
chromadb>=0.5.0

# --- LangChain / LangGraph ---
langchain>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0
langgraph>=0.2.0

# --- Ollama ---
ollama>=0.3.0

# --- 이미지 중복 제거 ---
imagehash>=4.3.0

# --- TTS (Kokoro/Coqui) ---
# Kokoro-82M 또는 Coqui TTS - 별도 설치 절차 필요
# kokoro-onnx 또는 coqui-tts 패키지 사용 시 아래 주석 해제
# kokoro-onnx>=0.1.0
# TTS>=0.22.0

# --- 유틸리티 ---
pydantic>=2.9.0
python-multipart>=0.0.9
